"""11. 고급 과제 B — 지연·중복 응답이 섞인 DAG 재시도와 예산 예약.

가르치는 것:
- 상태 합류는 도착 순서와 무관해야 한다: 논리 작업 ID와 시도 번호가 권위이며,
  늦게 도착한 이전 성공이 최신 상태를 되살리면 안 된다. 순서를 섞어도 같은 결과가
  나오는 reducer를 직접 증명한다.
- 배정은 예약이다: 동시성 슬롯·잔여 호출·최종 합류 예산을 한 판단에서 함께 계산해
  두 Worker가 같은 잔여 예산을 보고 중복 실행하는 경합을 막는다.
- 멱등과 충돌의 구분: 동일 관측의 재전달은 조용히 받아들이고, 같은 시도의 상충
  결과는 거절한다. "마지막 쓰기 승리"가 언제 위험한지가 이 과제의 핵심 질문이다.

출발점: 06의 revision 접두사, common/contracts.py의 merge_results.
실패 상황: 이전 시도의 성공 응답이 늦게 와 최신 timeout을 덮고 후속 작업을 실행한다.
또는 두 배정자가 같은 잔여 예산을 보고 동일 작업을 각각 실행한다.

구현 순서:
1. student_tasks.merge_attempts로 논리 작업 ID와 시도 번호를 분리한다.
2. 실행 순서를 섞어도 같은 최신 상태가 나오는 reducer를 만든다.
3. schedule_ready에서 선행 성공/동시성 슬롯/최종 검토 예산을 함께 확인한다.
4. 실제 Supervisor의 배정과 running 상태 기록에 연결한다. 완료한 업무를 다른
   이름으로 재제안하는 모델 계획까지 막으려면 안정적인 업무 식별 정책도 필요하다.

관측: worker 시작 횟수, 시도별 최종 채택 여부, 완료 작업 보존, 최종 답변의 근거.
공개 테스트는 결정 논리를 다룬다. 분산 exactly-once 보장을 검증하는 도구가 아니다.
"""

from common.challenges import run_challenge

if __name__ == "__main__":
    run_challenge(
        "과제 B: 재시도·의존성·예산을 함께 관리하는 Supervisor",
        __doc__,
        "test_recovery_challenge.py",
    )
