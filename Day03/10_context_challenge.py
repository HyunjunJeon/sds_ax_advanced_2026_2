"""10. 고급 과제 A — 예산 제한과 조항 의존성을 동시에 만족하는 Context 선택.

가르치는 것:
- Context 선택은 정렬이 아니라 제약 충족 조합 최적화다. 점수/길이 greedy가 중복
  커버 앞에서 실패하는 반례를 만들어 보고, 의존성 폐쇄까지 지키는 탐색을 설계한다.
- 예산의 실제 단위는 렌더링 바이트다. 한글 UTF-8, ID, 개행까지 세지 않으면 비용
  함수를 우회하는 해법이 통과한 것과 같다.
- 목적함수의 우선순위(커버 최대 → 바이트 최소 → ID 사전순)와 부분해: 완전 커버가
  불가능할 때 예산 내 최선해를 반환하고 누락이 답변에 남아야 한다.

출발점: 01의 단순 검색과 common/service.py의 context.
실패 상황: 길이가 짧고 점수가 높은 비슷한 근거를 반복 선택해 추가 약정을 놓친다.
더 작은 예산에서 숫자만 남기고 적용 조건을 버리면 바이트는 줄지만 답변은 틀린다.

구현 순서:
1. student_tasks.select_context의 제약/목적함수를 읽고 실패 반례부터 만든다.
2. requires의 전이적 폐쇄를 보존하며 중복 커버와 바이트 비용을 함께 최적화한다.
3. 실제 Evidence를 ContextItem에 연결하고 ID/URI/행/hash를 그대로 유지한다.
4. 동일 질문/초기 코퍼스에서 현재 선택기와 비교한다. 과거 gold를 런타임에 읽지 않는다.

공개 테스트 통과는 알고리즘 계약만 검증한다. 실제 원문의 의미상 필수 관계와
품질·토큰 개선은 별도 실험과 사람의 근거 대조가 필요하다. WORKSHEET A를 따른다.
"""

from common.challenges import run_challenge

if __name__ == "__main__":
    run_challenge(
        "과제 A: 의존성을 보존하는 Context 선택",
        __doc__,
        "test_context_challenge.py",
    )
